{
  "name": "Conda_env",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "echo 'dentro'; pwd; ls ;bash valva.sh",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-python.python"
  ]
}
